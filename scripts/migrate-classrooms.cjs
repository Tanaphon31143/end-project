const mysql = require("mysql2/promise");
async function main(){const url=new URL(process.env.DATABASE_URL);const connection=await mysql.createConnection({host:url.hostname,port:Number(url.port||3306),user:decodeURIComponent(url.username),password:decodeURIComponent(url.password),database:url.pathname.slice(1),ssl:{rejectUnauthorized:process.env.DATABASE_SSL_REJECT_UNAUTHORIZED==="true"},multipleStatements:true});await connection.query(`ALTER TABLE classrooms ADD COLUMN IF NOT EXISTS room_number INT UNSIGNED NULL; ALTER TABLE classrooms ADD COLUMN IF NOT EXISTS academic_year VARCHAR(10) NOT NULL DEFAULT '2569'; ALTER TABLE classrooms ADD COLUMN IF NOT EXISTS semester TINYINT UNSIGNED NOT NULL DEFAULT 1; ALTER TABLE classrooms ADD COLUMN IF NOT EXISTS is_active BOOLEAN NOT NULL DEFAULT TRUE; ALTER TABLE classrooms ADD COLUMN IF NOT EXISTS note VARCHAR(255) NULL; ALTER TABLE classrooms ADD COLUMN IF NOT EXISTS updated_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP; UPDATE classrooms SET room_number=CAST(SUBSTRING_INDEX(name,'/',-1) AS UNSIGNED) WHERE room_number IS NULL AND name LIKE '%/%';`);await connection.end();console.log("Classroom columns added without deleting existing data.")}
main().catch(error=>{console.error(error.message);process.exit(1)});
