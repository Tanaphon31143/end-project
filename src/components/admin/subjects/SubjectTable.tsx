"use client";
import { Pencil, Trash2 } from "lucide-react";
import type { SubjectRecord } from "./types";
export default function SubjectTable({subjects,onEdit,onDelete,busyId}:{subjects:SubjectRecord[];onEdit:(s:SubjectRecord)=>void;onDelete:(s:SubjectRecord)=>void;busyId:number|null}){return <section className="dashboard-card admin-table-card"><div className="card-head"><h2>รายการรายวิชา</h2><span className="row-count">ทั้งหมด {subjects.length} รายการ</span></div><div className="admin-data-wrap"><table><thead><tr><th>รหัสวิชา</th><th>ชื่อวิชา</th><th>ครูผู้สอน</th><th>ชั้นเรียน</th><th>จำนวนผู้เรียน</th><th>จัดการ</th></tr></thead><tbody>{subjects.length?subjects.map(subject=><tr key={subject.databaseId}><td><b className="subject-code">{subject.subjectCode}</b></td><td>{subject.subjectName}</td><td>{subject.teacherName}</td><td>{subject.className}</td><td>{subject.studentCount} คน</td><td><div className="table-actions"><button onClick={()=>onEdit(subject)} aria-label={`แก้ไข ${subject.subjectName}`} title="แก้ไข"><Pencil size={16}/><span>แก้ไข</span></button><button className="danger" disabled={busyId===subject.databaseId} onClick={()=>onDelete(subject)} aria-label={`ลบ ${subject.subjectName}`} title="ลบ"><Trash2 size={16}/><span>{busyId===subject.databaseId?"กำลังลบ":"ลบ"}</span></button></div></td></tr>):<tr><td colSpan={6}><div className="subject-empty"><span>ยังไม่มีรายวิชา</span><p>กด “เพิ่มรายวิชา” เพื่อเริ่มต้นเพิ่มข้อมูล</p></div></td></tr>}</tbody></table></div></section>}
