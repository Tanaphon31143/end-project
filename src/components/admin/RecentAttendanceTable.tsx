import Link from "next/link";
import { recentAttendance } from "@/data/dashboard";

export function RecentAttendanceTable() { return <section className="dashboard-card table-card"><div className="card-head"><div><h2>รายการเข้าเรียนล่าสุด</h2><p>อัปเดตการสแกนใบหน้าแบบเรียลไทม์</p></div><span className="live"><i/>กำลังอัปเดต</span></div><div className="attendance-table-wrap"><table><thead><tr><th>รหัสนักเรียน</th><th>ชื่อ-สกุล</th><th>ชั้น</th><th>เวลา</th><th>สถานะ</th></tr></thead><tbody>{recentAttendance.map((row) => <tr key={row.id}><td>{row.id}</td><td><div className="student-name"><span className={`student-avatar ${row.color}`}>{row.initials}</span><b>{row.name}</b></div></td><td>{row.room}</td><td>{row.time}</td><td><span className={`status-badge ${row.status === "มาเรียน" ? "present" : row.status === "สาย" ? "late" : "absent"}`}>{row.status}</span></td></tr>)}</tbody></table></div><Link className="view-all" href="/admin/attendance">ดูทั้งหมด <span>→</span></Link></section>; }
