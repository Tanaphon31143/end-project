import type { ResultSetHeader,RowDataPacket } from "mysql2";import { getAdminSession } from "@/lib/auth";import { getClassroomPageData } from "@/lib/admin-data";import { db } from "@/lib/db";
type Payload={id?:number;className?:unknown;gradeLevel?:unknown;roomNumber?:unknown;advisorTeacherId?:unknown;academicYear?:unknown;semester?:unknown;isActive?:unknown;note?:unknown};
async function admin(){return Boolean(await getAdminSession())} function parse(b:Payload){const className=String(b.className||"").trim(),gradeLevel=String(b.gradeLevel||"").trim(),roomNumber=String(b.roomNumber||"").trim(),advisorTeacherId=Number(b.advisorTeacherId);if(!className)return{error:"กรุณากรอกชื่อห้องเรียน"};if(!gradeLevel)return{error:"กรุณาเลือกระดับชั้น"};if(!/^\d+$/.test(roomNumber)||Number(roomNumber)<1)return{error:"หมายเลขห้องต้องเป็นจำนวนเต็มบวก"};if(!advisorTeacherId)return{error:"กรุณาเลือกครูที่ปรึกษา"};return{value:{className,gradeLevel,advisorTeacherId}}}
export async function GET(){if(!(await admin()))return Response.json({message:"ไม่มีสิทธิ์ใช้งาน"},{status:401});return Response.json(await getClassroomPageData())}
export async function POST(r:Request){if(!(await admin()))return Response.json({message:"ไม่มีสิทธิ์ใช้งาน"},{status:401});const p=parse(await r.json());if("error"in p)return Response.json({message:p.error},{status:400});const [dup]=await db.execute<RowDataPacket[]>("SELECT id FROM classrooms WHERE name=?",[p.value.className]);if(dup.length)return Response.json({message:"ห้องเรียนนี้มีอยู่ในระบบแล้ว"},{status:409});await db.execute<ResultSetHeader>("INSERT INTO classrooms(name,level,advisor_teacher_id) VALUES(?,?,?)",[p.value.className,p.value.gradeLevel,p.value.advisorTeacherId]);return Response.json({message:"เพิ่มห้องเรียนสำเร็จ"},{status:201})}
export async function PUT(r:Request){if(!(await admin()))return Response.json({message:"ไม่มีสิทธิ์ใช้งาน"},{status:401});const b=await r.json() as Payload,p=parse(b),id=Number(b.id);if("error"in p)return Response.json({message:p.error},{status:400});try{const [result]=await db.execute<ResultSetHeader>("UPDATE classrooms SET name=?,level=?,advisor_teacher_id=? WHERE id=?",[p.value.className,p.value.gradeLevel,p.value.advisorTeacherId,id]);if(!result.affectedRows)return Response.json({message:"ไม่พบห้องเรียน"},{status:404});return Response.json({message:"แก้ไขห้องเรียนสำเร็จ"})}catch{return Response.json({message:"ห้องเรียนนี้มีอยู่ในระบบแล้ว"},{status:409})}}
export async function DELETE(r:Request){if(!(await admin()))return Response.json({message:"ไม่มีสิทธิ์ใช้งาน"},{status:401});const id=Number(new URL(r.url).searchParams.get("id"));const [count]=await db.execute<(RowDataPacket&{total:number})[]>("SELECT COUNT(*) total FROM students WHERE class_id=?",[id]);if(count[0]?.total)return Response.json({message:"ไม่สามารถลบห้องเรียนที่ยังมีนักเรียนอยู่ได้ กรุณาย้ายนักเรียนออกจากห้องก่อน"},{status:409});await db.execute("DELETE FROM classrooms WHERE id=?",[id]);return Response.json({message:"ลบห้องเรียนสำเร็จ"})}
